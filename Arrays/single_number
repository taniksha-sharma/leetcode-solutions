class Solution {
    public int singleNumber(int[] nums) {
        int ans = 0 ;

        for (int num : nums){
            ans ^= num;  //XOR cancels duplication 
            //we use XOR pattern when every element appears twice except for one 
        }
        return ans;
        
    }
}